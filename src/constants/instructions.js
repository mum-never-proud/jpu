const MOV_LIT_REG = 0x01;
const MOV_REG_REG = 0x02;
const MOV_MEM_REG = 0x03;
const MOV_REG_MEM = 0x04;

const ADD_REG_REG = 0x10;

const JMP_NOT_EQ = 0x20;

export {
  MOV_LIT_REG,
  MOV_REG_REG,
  MOV_MEM_REG,
  MOV_REG_MEM,
  ADD_REG_REG,
  JMP_NOT_EQ,
};
